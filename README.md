# Testing

The client and server are separate packages (`raid-race` and `raid-race-server` respectively). Once both are running, open the console in the game window with `` ` `` and run `connect localhost` to connect to the server.
