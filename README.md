# Testing

The client server, and auth server are separate packages (`raid-race`, `raid-race-server`, and `raid-race-auth` respectively). Once all are running, open the console in the game window with `` ` `` and run `connect localhost` to connect to the server.
